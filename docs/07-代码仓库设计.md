---
title: "007 - TgBuddy 代码仓库设计"
description: "TgBuddy 模块职责、依赖方向、Runtime 门面、Composition Root 与分阶段迁移规则。"
category: "design"
number: "007"
status: "active"
services: ["src/shared", "src/runtime", "src/kernel/pi", "src/infrastructure", "src/main", "src/preload", "src/renderer"]
related: ["design/001", "design/002", "design/006"]
last_modified: "2026-07-30"
---

# 007 - TgBuddy 代码仓库设计

## Status

这是 TgBuddy 第一版的代码归属与依赖基线。产品语义仍以 `docs/01-架构设计.md`、`docs/02-功能范围.md` 和 `docs/06-设计决策.md` 为准；本文只回答“代码放在哪里、谁可以依赖谁、迁移期间如何防止边界回退”。

Phase 1 的第一步已经建立公共 contract、`TgBuddyRuntime` 门面、显式 Composition Root 和自动架构检查。SQLite SessionRepo、`AgentHarness`、Workspace、安全、Blob、能力系统与 Renderer feature 仍按后续 Story 逐步迁移。

## Summary

仓库采用“领域模块化 Runtime + 边缘 Adapter + 显式 Composition Root”：

```text
Renderer ──typed IPC──> Electron Main ──> TgBuddyRuntime
                                            │
                     ┌──────────────────────┼──────────────────────┐
                     ▼                      ▼                      ▼
                Kernel ports          Persistence ports      Host ports
                     │                      │                      │
                pi adapters           SQLite / Blob          MCP / secrets /
                                                            filesystem
```

- `shared/contracts` 保存跨进程、跨边界的可序列化 DTO、事件和 IPC map。
- `runtime` 拥有业务规则和用例，不知道 Electron、React、Node 文件系统、SQLite 或 pi。
- `kernel/pi` 是 pi 运行时唯一边界。
- `infrastructure` 实现 SQLite、Blob、文件系统、Secret 和 MCP transport 端口。
- `main` 只负责 Electron 生命周期、窗口、IPC 与依赖装配。
- `preload` 只把 typed IPC 暴露为 `window.tgbuddy`。
- `renderer` 只依赖 shared contract 和前端模块。

## 1. 依赖方向

```text
shared <- runtime
shared + runtime ports <- kernel/pi
shared + runtime ports <- infrastructure
shared + runtime public API + kernel/infrastructure factories <- main/bootstrap
shared + runtime public API <- main/ipc
shared <- preload
shared <- renderer
```

具体规则由 `scripts/check-architecture.ts` 执行：

| Source | Allowed targets | 关键禁止项 |
|---|---|---|
| `src/shared/**` | shared；两个指定 contract 的 pi type-only 例外 | runtime、kernel、infrastructure、main、Electron、React、Node runtime |
| `src/runtime/**` | shared、runtime | pi、Electron、React、Node runtime、kernel、infrastructure、main |
| `src/kernel/pi/**` | shared、runtime port、pi、必要 Node API | infrastructure、main、preload、renderer、Electron |
| `src/infrastructure/**` | shared、runtime port、必要 Node/Electron adapter API | kernel、main、preload、renderer、Runtime concrete service |
| `src/main/bootstrap/**` | shared、Runtime public API、factory、main 内部、Electron/Node | renderer、preload |
| `src/main/ipc/**` | shared、Runtime public API、纯 IPC helper、Electron | Runtime 内部、kernel、infrastructure、repository/store |
| Main lifecycle/window | shared、Runtime public API、main 内部、Electron/Node | renderer、preload、concrete repository |
| `src/preload/**` | shared、Electron context bridge | runtime、kernel、infrastructure、main、renderer |
| `src/renderer/**` | shared、renderer、前端 package | runtime、kernel、infrastructure、main、preload、Electron、Node runtime |

检查范围固定为 `src/**/*.{ts,tsx}`，排除 `.d.ts`、`dist/**` 和 `node_modules/**`。相对路径、`.ts/.tsx` 后缀、目录 `index.ts`、TypeScript paths 和 Vite alias 都必须解析。每条违规输出：

```text
source -> target -> violated rule
```

本地与 CI gate 使用：

```bash
bun run check:architecture
```

## 2. 公共契约

`src/shared/contracts` 按 workspace、session、run、message、event、permission、capability、artifact、channel、context 和 IPC 拆分。contract 必须可序列化，不得包含 Electron handle、数据库连接、文件描述符或 service 实例。

消息继续使用 pi 原始消息信封和 `kernel: 'pi@0.82'` 版本护栏，不增加往返翻译层。只有以下 type-only import 例外：

- `contracts/message.ts` 可引用 `@earendil-works/pi-ai` 和 `@earendil-works/pi-agent-core` 的消息类型。
- `contracts/events.ts` 可引用 pi 的 `StopReason` 与 `Usage`。

`IpcCommandMap` 是请求/响应的唯一类型来源。Preload 的友好 API 使用 `satisfies TgBuddyAPI` 校验，Main handler 使用同一 map 的 `IpcRequest` / `IpcResponse`。

## 3. Runtime 门面

宿主只能通过 `TgBuddyRuntime` 调用应用能力：

- `workspaces`
- `sessions`
- `runs`
- `permissions`
- `plans`
- `questions`
- `context`
- `artifacts`
- `capabilities`
- `settings`
- `subscribe(listener)`
- `dispose()`

Runtime 接收 `RuntimeDependencies` 显式构造，不读取模块级 service locator，不创建 Electron 窗口、数据库或文件系统实例。业务事件统一通过 `RuntimeEventListener` 发布，IPC 只负责转发。

## 4. Composition Root

`src/main/bootstrap/create-application.ts` 是唯一 Composition Root。后续 Story 在这里装配 SQLite repositories、BlobStore、SecretStore、MCP transport、`PiAgentEngine` 与 Runtime。

当前 `create-legacy-runtime.ts` 只用于分阶段迁移：

- Story 1B 删除 Session/channel 存储委托并切换 SQLite canonical。
- Story 1C 删除 orchestrator、permission、plan、question 与 compaction 委托。
- 它不得新增产品入口，也不能成为第二个长期门面。

Electron 退出前调用 `application.dispose()`；IPC handler 不再直接 import store、repository 或 kernel。

## 5. Alias

TypeScript 与 Vite 的同名 alias 必须指向相同目录：

| Alias | Target |
|---|---|
| `@` | `src` |
| `@runtime` | `src/runtime` |
| `@shared` | `src/shared` |

架构 checker 会把同名 alias 的分歧作为违规，避免 TypeScript 能通过但 Vite 解析到另一棵目录。

## 6. 分阶段迁移

| Phase | Story | 主要删除的旧 owner |
|---|---|---|
| Phase 1A | 仓库边界与 Runtime 门面 | IPC service locator |
| Phase 1B | SQLite SessionRepo | JSONL canonical、旧 session/channel store 委托 |
| Phase 1C | AgentHarness/Run 生命周期 | 裸 orchestrator、旧 compaction 与 Main pi 调用 |
| Phase 2 | Workspace 与安全 | 全局 sandbox、旧权限 owner |
| Phase 3 | Blob 与产物 | 消息内大输出、旧 artifact 推导 |
| Phase 4 | 能力系统 | Main 内工具/渠道/Skill/MCP owner |
| Phase 5 | 单层 child | 临时 delegation 状态 |
| Phase 6 | UI 与恢复验收 | `shared/types` re-export、总 atom/总监听器 |

兼容层必须同时满足三条：

1. 只委托旧实现，不复制业务规则。
2. 在代码和本文中写明删除 Story。
3. 新功能不得继续接入兼容层。

## 7. 验证门槛

每个 Story 至少执行相关单测、`bun run check:architecture`、`bun run typecheck`、`bun test` 和 `bun run build`。架构检查与功能测试缺一不可：前者阻止依赖回退，后者证明迁移没有改变行为。

## References

- `docs/01-架构设计.md`
- `docs/02-功能范围.md`
- `docs/06-设计决策.md`
- `.ship/tasks/target-code-repository-architecture/plan/spec.md`
- `.ship/tasks/target-code-repository-architecture/plan/plan.md`
