# Electron 二进制从 GitHub Releases 下载，国内直连极慢或超时。
# 走 npmmirror 镜像。若失败，清掉 node_modules/electron/dist 和 path.txt 后重试
# （install.js 看到空的 dist 目录会误判为已安装，静默退出 0，既不下载也不报错）。
ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
